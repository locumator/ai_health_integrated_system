{
  "name": "ai-health-integrated-system",
  "version": "1.0.0",
  "description": "FastAPI application for AI health message drafting",
  "scripts": {
    "start": "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  },
  "engines": {
    "python": "3.12.7"
  }
}

